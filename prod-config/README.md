# prod-config/

**ACCÈS INTERDIT pour Claude Code.**

Tout fichier ici est bloqué par `permissions.deny` dans `.claude/settings.json`
ET par le hook `block-sensitive-paths.sh`. Sert à démontrer la défense en profondeur.
